[project]
name = "api-call-matrix"
version = "0.1.0"
description = "Matrix visualisation of documents generated by stable diffusion pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.3",
    "flask",
]
